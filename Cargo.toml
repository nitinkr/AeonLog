[package]
name = "aeonlog"
version = "0.1.0"
authors  = ["nitin kumar"]
description = "A rust bitcask impl. A KV store."
edition = "2024"

[dev-dependencies]
assert_cmd = "0.11.0"
predicates = "1.0.0"

[dependencies]
clap = { version = "4.6.7", features = ["derive"] }
